{
  "name": "static-site",
  "version": "1.0.0",
  "description": "## 1. Make a simple landing page 2. Make a template for a blog post 3. Make a Markdown -> HTML converter 4. Simple integration for ConvertKit 5. Add a contact form",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/build.js",
    "serve": "npm run build && serve dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "front-matter": "^4.0.2",
    "fs-extra": "^11.3.0",
    "marked": "^15.0.6"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "serve": "^14.2.4"
  }
}
